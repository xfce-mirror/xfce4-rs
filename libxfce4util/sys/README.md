# libxfce4util-sys

This crate provides FFI bindings for `libxfce4util`, a utility library
for Xfce applications.

In general you will not need to use this crate, and will instead want to
use [xfce4-util](https://crates.io/crates/xfce4-util) instead.
